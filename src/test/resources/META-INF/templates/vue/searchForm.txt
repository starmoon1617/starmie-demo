<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">

<body>
    <v-layout th:fragment="searchForm">
        <v-container fluid>
            [# th:with="setResult=${counter.set('cnt', 0)}"][# th:unless="${#lists.isEmpty(columns)}"][# th:each="column, st : ${columns}"][# th:unless="${(#strings.equals(column.property,'createBy')) || (#strings.equals(column.property,'createTime')) || (#strings.equals(column.property,'updateBy')) || (#strings.equals(column.property,'updateTime')) }"][# th:if="${counter.get('cnt') % 3 == 0}"]
            <v-row>[/]
                <v-col cols="1" sm="6" md="3">
                    <v-text-field v-model="searchForm._FC1_[(${#strings.substring(column.javaType, 0,1)})]0_[(${column.property})]" label="[(${column.remark})]" outlined dense> </v-text-field>
                </v-col>
            [# th:if="${(counter.get('cnt') % 3 == 2) || st.last }"]</v-row>[/][# th:with="setResult=${counter.set('cnt', (counter.get('cnt') + 1))}"][/][/][# th:if="${(counter.get('cnt') % 3 != 2) && st.last }"]
            </v-row>[/][/][/][/]
        </v-container>
    </v-layout>
</body>

</html>